{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Fretboard",
            "type": "cppdbg",
            "request": "launch",
            "preLaunchTask": "Compile",
            "cwd": "${workspaceFolder}/build",
            "stopAtEntry": false,
            "expressions": "native",
            "program": "${workspaceFolder}/build/fretboard"
        }
    ]
}
